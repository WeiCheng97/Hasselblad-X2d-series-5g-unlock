# X2D / 907 WiFi 地区设置工具（开源本地版）— 使用说明与问题指南

本工具用于解除哈苏 X2D / X2D II / 907X & CFV 100C 日版相机的 WiFi 5GHz 限制（日版出厂仅有 2.4GHz），也可随时恢复日版。
**不刷机、不改固件、完全可逆。**

适用机型：X2D 100C、X2D II 100C（已实测）、907X & CFV 100C（社区实测，热点名形如 `CFV 100C xxxxxx`）。

**完全本地运行**：不连任何服务器、无授权码、无配额、无遥测，下载即用。

---

## 一、四种形态，任选一个

| 形态 | 文件 | 平台 | 说明 |
|---|---|---|---|
| Mac 图形版 | `X2D_WiFi_Region_mac.zip` | macOS 12+（Intel / Apple Silicon） | 解压后双击运行；首次打开未签名 app：右键 → 打开，或在「系统设置 → 隐私与安全性」里允许 |
| Mac 双击极简版 | `X2D_WiFi_Region_mac.command` | macOS | 双击即用，系统自带运行环境，无需安装任何东西 |
| Windows 图形版 | `X2D_WiFi_Region_win.exe` | Windows 10/11 x64 | 单文件直接运行。⚠️ **未经真机测试**，遇到问题请改用 Python 脚本 |
| Python 命令行 | `src/x2d_wifi_region.py` | 跨平台 | 无第三方依赖，最稳妥的备选 |

---

## 二、使用流程（图形版）

| 步骤 | 操作 | 说明 |
|---|---|---|
| 1 | **相机打开 WiFi 热点**：相机菜单 → WiFi → 热点 | 热点名和密码直接显示在相机屏幕上 |
| 2 | 打开电脑上的工具 | 工具会**自动扫描**周边相机热点并把热点名填进下拉框；多台相机可在下拉中选择 |
| 3 | 输入热点密码 | 以**相机屏幕显示**为准。输一次后**本机永久记住**，之后自动带出 |
| 4 | 点要执行的操作 | 「查看当前地区」（只读，安全）/「设为 CN 解锁 5GHz」/「设为 JP 恢复」。点设置类操作前会弹一次确认 |
| 5 | 等待自动执行完成 | 写入地区 → 相机**自动重启** → 工具自动切回你的日常 WiFi |
| 6 | 相机重启完成后 | 新地区生效（CN = 解锁 5GHz） |

> 全程约 3 分钟，第 5 步之后无需人工干预。

## 三、使用流程（Python 命令行）

先在系统设置里手动连上相机热点，然后：

```bash
python3 src/x2d_wifi_region.py                    # 只读当前地区
python3 src/x2d_wifi_region.py --set 6            # 设为 CN（解锁 5G）
python3 src/x2d_wifi_region.py --set 8            # 设为 JP（恢复日版）
python3 src/x2d_wifi_region.py --set 6 --reboot   # 写入并自动重启相机
python3 src/x2d_wifi_region.py --host 192.168.2.1 # 指定相机 IP
```

---

## 四、常见问题与处置

### 1. macOS 打不开 app（提示未签名 / 来自 unidentified 开发者）

| 原因 | 解决办法 |
|---|---|
| app 未做 Apple 签名 | 右键 app →「打开」；或在「系统设置 → 隐私与安全性」里点「仍要打开」 |

### 2. macOS 弹窗请求「位置」权限

| 原因 | 解决办法 |
|---|---|
| 系统要求：读取 WiFi 名称 / 扫描热点需要位置权限 | 系统弹窗点「允许」即可，属正常需求 |

### 3. 相机屏幕弹「是否允许此设备」

- 点**允许**即可（首次连接或新设备时可能出现）。

### 4. 连不上相机（30303 端口不通）

| 可能原因 | 解决办法 |
|---|---|
| 相机热点没开 | 相机菜单 → WiFi → 热点，确认已开启（相机休眠时热点会关闭，重新打开） |
| 电脑没连上相机热点 | 检查电脑当前 WiFi 是否是相机热点 |
| 相机 IP 不对 | Python 版用 `--host` 参数指定（默认 192.168.2.1） |

### 5. 连接热点失败（报错 -3925 等）

| 可能原因 | 解决办法 |
|---|---|
| 密码输错 | 以**相机屏幕上显示的密码**为准重新输入 |
| 热点没开 | 相机上重新开启热点 |
| 系统里存了旧的错误连接记录 | 新版工具会先自动清理旧记录再连；也可在系统 WiFi 设置里手动连上热点后，点工具里的「**我已手动连好**」 |

### 6. 连上了热点却检测不到相机

| 可能原因 | 解决办法 |
|---|---|
| 刚连上热点，相机服务尚未就绪 | 等几秒再试 |
| 持续检测不到 | 查看日志：Mac 在 `~/Library/Logs/x2d-region-local.log` |

### 7. 改完之后没生效（WiFi 里看不到 5GHz）

| 可能原因 | 解决办法 |
|---|---|
| 地区参数需相机**完全重启后**才重新读取 | 工具已自动触发重启；如过程被中断，手动将相机完全重启一次即可 |

### 8. Windows 版打不开 / 连不上 / 闪退

| 原因 | 解决办法 |
|---|---|
| Windows 版 GUI 尚未经真机测试（README 已明示） | 改用 Python 命令行脚本完成同样操作：在系统设置里手动连上相机热点，然后运行 `python3 src/x2d_wifi_region.py --set 6 --reboot` |

---

## 五、常见问题（其他）

**Q：会变砖吗？**
不会。走的是相机原生工厂诊断命令（产测通道：TCP 30303 → testd → ProdConfig），参数在合法值范围内随时可改回。

**Q：工具会联网吗？**
不会。完全本地运行，不连任何服务器、无授权码、无配额、无遥测。

**Q：如何恢复日版？**
同样的流程，选「设为 JP 恢复」即可，完全可逆。

**Q：支持哪些机型？**
Hasselblad X2D 100C（固件 v4.2.0 实测）、X2D II 100C（v1.3.16.2 实测）、907X & CFV 100C（v4.0.0，社区实测）。其他固件版本理论一致，未逐一实测。

---

## 六、附注

- 原理：相机自带产测通道（TCP 30303 → testd → ProdConfig，属性 13 = wifiRegion）读写 WiFi 地区参数；协议细节见开源仓库 README，全部公开。
- 日版（JP）会被系统双层禁用 5G（相机 GUI 隐藏 5G 菜单 + 网络服务强制 2.4G-only），改为 CN 后即解锁。
- 日志位置（Mac GUI）：`~/Library/Logs/x2d-region-local.log`，反馈问题时请附上。
- 本工具仅用于恢复/调整你自己相机的合法射频配置，请遵守所在地区的无线电法规。与 Hasselblad 无任何关联；Hasselblad、X2D 为各自权利人商标。
- License：MIT。
